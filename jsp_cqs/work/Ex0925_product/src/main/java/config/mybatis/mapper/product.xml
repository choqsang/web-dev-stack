<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="p">

	<select id="product_list" parameterType="String" resultType="pro">
		select * from product where category = #{category}
	</select>
	
	<insert id="product_insert" parameterType="pro">
		insert into product 
		values(
			seq_pro_idx.nextVal,
			#{category}, 
			#{p_num}, 
			#{p_name}, 
			#{p_company}, 
			#{p_price}, 
			#{p_saleprice}, 
			#{p_image_s}, 
			#{p_image_l},
			#{p_content},
			sysdate
		)
	</insert>
	
	<select id="product_detail" parameterType="int" resultType="pro">
		select * from product where idx= #{idx}
	</select>
	
	<update id="product_update_fin" parameterType="pro">
		update product
			set
				category = #{category}, 
				p_num = #{p_num}, 
				p_name = #{p_name}, 
				p_company = #{p_company}, 
				p_price = #{p_price}, 
				p_saleprice = #{p_saleprice}, 
				p_image_s = #{p_image_s}, 
				p_image_l = #{p_image_l},
				p_content = #{p_content}
			where
				idx = #{idx}
	</update>

</mapper>












