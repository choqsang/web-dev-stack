<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="d">

	<select id="dept_select" resultType="vo.DeptVO">
		select * from dept
	</select>
	
	<insert id="insert_dept" parameterType="vo.DeptVO">
		insert into dept values(#{deptno}, #{dname}, #{loc})
	</insert>
	
	<delete id="delete_dept" parameterType="int">
		delete from dept where deptno = #{deptno}
	</delete>

	<select id="update_one" parameterType="int" resultType="vo.DeptVO">
		select * from dept where deptno = #{deptno}
	</select>
	
	<update id="update_dept_fin" parameterType="java.util.Map">
		update dept 
			set deptno = #{deptno}, 
				dname = #{dname}, 
				loc = #{loc}
		where deptno = #{ori_deptno}
	</update>

</mapper>












